extends: "configs/base_model.yaml"

experiment:
  name: "token_markov_grpo"
  state_mode: "token_markov"

training:
  # TODO: Align with configs/final_parity/base_parity.yaml + PROJECT_CONTRACT.md (Yuan = hints).
  seed: 11
